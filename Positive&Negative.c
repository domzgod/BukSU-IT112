int main()
{
    int domz;
        printf("Enter a number:");
        scanf("%d",&domz);

        if (domz >= 0)
            printf("%d is a positive number.", domz);
        else
            printf("%d is a negative number.", domz);
        return 0;
}
