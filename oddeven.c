int main()
{
    int domz;
    printf("Enter a number:");
    scanf("%d",&domz);

    if(domz % 2 == 0)
        printf("%d is even number.", domz);
    else
        printf ("%d is an odd number.", domz);
    return 0;
}
